import { Task } from '@ohos/hvigor-base/src/external/task/task.js';
import { HvigorNode } from '@ohos/hvigor-base/src/external/core/hvigor-node.js';
/**
 * 根据DAG图,将传入的参数递归的执行所有任务和其依赖
 *
 * @since 2022/4/22
 */
export declare class TaskExecutor {
    /**
     * 通过指定task的path，执行对应的任务
     *
     * @param {string[]} taskPaths 需要执行的所有任务路径
     */
    executeTaskByName(taskPaths: string[]): void;
    /**
     * 执行对应的task
     *
     * @param {Task[]} tasks 需要执行的任务
     */
    execute(tasks: Task[]): void;
    private static executeOneTask;
}
export declare const taskExecutorInst: TaskExecutor;
/**
 * 通过Task的path分割获取其所属Module
 *
 * @param {string} taskPath
 * @returns {HvigorNode | undefined}
 */
export declare function getModuleFromTaskPath(taskPath: string): HvigorNode | undefined;
